/**
 * Camera verification: the user's consent and settings. Frames never reach this package; the
 * local companion program reports only observations (requirements specification, section 21).
 */
package com.example.focusquest.vision;
