spring:
  application:
    name: focusquest-backend

  datasource:
    url: jdbc:h2:file:./data/focusquest;AUTO_SERVER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password: ""

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  h2:
    console:
      # Off by default: it exposes the whole database. For local debugging start the backend with
      # FOCUSQUEST_H2_CONSOLE=true and open http://127.0.0.1:8080/h2-console
      enabled: ${FOCUSQUEST_H2_CONSOLE:false}
      path: /h2-console
      settings:
        web-allow-others: false

server:
  port: 8080
  address: 127.0.0.1

focusquest:
  jwt:
    # Set FOCUSQUEST_JWT_SECRET to a random string of at least 32 bytes to keep sign-ins across
    # restarts (for example: openssl rand -base64 48). Left unset, a random secret is generated
    # each run and everyone is signed out on restart. Never commit a real secret.
    secret: "${FOCUSQUEST_JWT_SECRET:}"
    expiration-minutes: 60
  # The XP and gem economy; see docs/technical_architecture.md, section 19 (XP and gem economy).
  xp:
    # XP for completing a session, per planned focus minute. Only completed sessions earn it.
    per-focus-minute: 1
    # XP the first time a streak period reaches its target.
    daily-streak-bonus: 10
    weekly-streak-bonus: 50
    # Deducted once for overriding blocking on an abandoned session. Equal to 10 focused minutes.
    manual-override-penalty: 10
  gems:
    # Gems granted for each level reached (from level 2).
    per-level: 5
    # Gems the first time a streak period reaches its target.
    daily-streak: 1
    weekly-streak: 5
  cors:
    allowed-origins:
      - http://localhost:5173
      # Replace with the extension's actual id from chrome://extensions after loading it unpacked.
      - chrome-extension://REPLACE_WITH_EXTENSION_ID

logging:
  level:
    root: INFO
    com.example.focusquest: DEBUG
    org.springframework.web: INFO
    org.hibernate.SQL: WARN
  pattern:
    console: "%d{HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n"
